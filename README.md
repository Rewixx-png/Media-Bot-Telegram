# 🛠 Media Bot (Native + Local API)

Высокопроизводительный Telegram-бот для обработки медиа. 
Написан на **Python (Aiogram 3)**. Использует **Local Telegram Bot API Server** для работы с файлами до 2 ГБ и мгновенной обработки без скачивания по сети.

## 🚀 Архитектура

Проект работает в **Гибридном режиме**:
1.  **Telegram Bot API Server** работает в Docker-контейнере и сохраняет файлы в папку на хосте.
2.  **Бот** работает нативно (Python Process) на хосте, имеет прямой доступ к этим файлам через файловую систему (Zero-Copy).

Это решает проблемы с таймаутами Nginx, SSL-сертификатами и скоростью скачивания.

## ✨ Возможности

- 📹 **Видео -> Кружок**: Конвертация любых видео/GIF в Video Note (360px).
- 🎵 **Аудио Конвертер**: WAV (с настройкой битности/частоты) / OGG (Voice).
- 🎬 **Видео -> Аудио**: Извлечение дорожки из видео.
- 🚀 **Local API**: Поддержка файлов до 2000 МБ.

## ⚙️ Установка

### 1. Требования
- Linux (Ubuntu/Debian)
- Python 3.10+
- FFmpeg (`sudo apt install ffmpeg`)
- Docker & Docker Compose (только для API сервера)

### 2. Запуск Local API Server (Docker)
Сервер должен быть запущен отдельно. Пример `docker-compose.yml` для API:
```yaml
services:
  telegram-bot-api:
    image: aiogram/telegram-bot-api:latest
    entrypoint: telegram-bot-api
    command: >
      --local --api-id=<YOUR_ID> --api-hash=<YOUR_HASH> --http-port=8081 --dir=/var/lib/telegram-bot-api
    volumes:
      - ./data:/var/lib/telegram-bot-api
    ports:
      - "127.0.0.1:8081:8081"
```
Запомните путь к папке `data` на хосте! (например: `/root/Bots/BotApi/data`).

### 3. Установка Бота
```bash
# Клонируем
git clone <repo_url>
cd Media-Bot-Telegram

# Устанавливаем зависимости
pip3 install -r requirements.txt
```

### 4. Настройка (.env)
Создайте файл `.env`:
```ini
BOT_TOKEN=123456:AAH...
ADMIN_ID=123456789

# Данные для API (опционально, если не используются в коде напрямую)
TELEGRAM_API_ID=...
TELEGRAM_API_HASH=...

# ГЛАВНОЕ: Путь к данным API сервера НА ХОСТЕ
LOCAL_API_PATH=/root/Bots/BotApi/data
```

### 5. Запуск
```bash
python3 main.py