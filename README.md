# README.md

# Media Bot: Мастерская Медиа для Telegram

Этот бот — мощный инструмент для работы с медиафайлами прямо в Telegram. Для обработки файлов большого размера (до 2 ГБ) он использует кастомный сервер Telegram Bot API.

## ✨ Возможности

- 📹 **Конвертация видео в "кружки"**: Превращает любые видео и GIF-анимации в видеосообщения (кружки).
- 🎵 **Конвертация аудио**: Поддерживает конвертацию аудио в разные форматы с гибкой настройкой качества:
  - **OGG (Opus)** для голосовых сообщений.
  - **WAV** с настройкой глубины бита (8/16/24-bit), частоты дискретизации (22-96 кГц) и каналов (моно/стерео).
- 🎬 **Извлечение аудио из видео**: Может вытащить звуковую дорожку из любого видеофайла и сохранить ее в форматах MP3, OGG или WAV.

## 🚀 Установка и Запуск (Docker)

Установка и запуск проекта выполняются с помощью Docker и Docker Compose, что обеспечивает изоляцию и простоту развертывания.

1.  **Установите Docker и Docker Compose** на ваш сервер.

2.  **Клонируйте репозиторий:**
    ```bash
    git clone <адрес_вашего_репозитория>
    cd Media-Bot-Telegram-master
    ```

3.  **Создайте и настройте `.env` файл:**
    Скопируйте пример `.env.example` в новый файл `.env`:
    ```bash
    cp .env.example .env
    ```
    Откройте `.env` и вставьте ваш токен бота вместо `ВАШ_API_ТОКЕН_ЗДЕСЬ`.

4.  **Соберите образ и запустите контейнер:**
    Эта команда скачает все зависимости, соберет образ и запустит бота в фоновом режиме.
    ```bash
    docker compose up --build -d
    ```
    Бот запущен и будет автоматически перезапускаться после сбоев или перезагрузки сервера.

## ⚙️ Управление ботом

Управление ботом осуществляется командами `docker compose`. Выполняйте их из директории проекта.

-   **Посмотреть логи в реальном времени:**
    ```bash
    docker compose logs -f
    ```

-   **Остановить бота:**
    ```bash
    docker compose down
    ```

-   **Перезапустить бота (например, после обновления кода):**
    ```bash
    docker compose restart
    ```

-   **Пересобрать образ и перезапустить (если изменился Dockerfile или зависимости):**
    ```bash
    docker compose up --build -d
    ```